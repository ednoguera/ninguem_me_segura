import React from "react"

const Header = (props) => {
    <header>
        <div>
            <div>
                <p>{props.title}</p>
            </div>

            <
        </div>
    </header>
}